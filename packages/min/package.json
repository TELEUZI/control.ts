{
  "name": "@control.ts/min",
  "description": "A minimal version of the control.ts library. Contains only base component and tags.",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc --noEmit --watch",
    "build": "vite build && tsc --project tsconfig.build.json --declaration --emitDeclarationOnly --outDir dist",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint": "eslint ./src",
    "lint:fix": "eslint --fix ./src",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@control.ts/eslint-config": "workspace:*",
    "@control.ts/typescript-config": "workspace:*",
    "@types/eslint": "^8.56.1",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0",
    "vite": "^4.1.2"
  },
  "exports": {
    "./src": "./src/index.ts",
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/min.js",
      "import": "./dist/min.mjs"
    }
  },
  "main": "./dist/min.js",
  "module": "./dist/min.mjs",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist/**"
  ],
  "publishConfig": {
    "access": "public"
  }
}
